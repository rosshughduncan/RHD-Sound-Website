import React from 'react';
import { CSSTransition } from "react-transition-group";

const Welcome = (props) => {
    return (
        <div>
            
        </div>
    );
};

export default Welcome;